[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
mock = "*"
pytest-cov = "*"
pytest-black = "*"
pytest-flake8 = "*"
flake8-import-order = "*"
mypy = "*"
sphinx_rtd_theme = "*"

[packages]

[scripts]
# Put the coverage options here, not setup.cfg, so PyCharm coverage still works
tests = "python -m pytest tests --cov=versiongit --cov-fail-under=90"
docs = "sphinx-build -b html docs build/html"
