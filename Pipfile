[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
mock = "*"
pytest-cov = "*"
pytest-flake8 = "*"
pytest-black = "*"
flake8-isort = "*"
sphinx_rtd_theme = "*"

[packages]

[scripts]
# Put coverage threshold here so we don't fail IDE tests
tests = "python -m pytest --cov-fail-under=90"
docs = "sphinx-build -b html docs build/html"

[pipenv]
allow_prereleases = true
